init();

function init() {
    nodeListArray();
}

function nodeListArray(){
    var elements = document.querySelectorAll("div");
//Create an array from the node list with .from()
    console.log(array1.from(elements));
}
