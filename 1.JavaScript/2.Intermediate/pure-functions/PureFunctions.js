const add = (x, y) => x + y;
console.log(add(5, 10));

const fullName = (name, lastName) => `${name} ${lastName}`;
console.log(fullName('brealy', 'padron'));

// We can replace the function with the output
// This is called "referencional transparency"

//Pure function hace at leat one parameter

//Otherwise, it is the same  as constant becuase they 
// can only work with their input.

// const firstName = () => "brealy";
// const firstName = "brealy";