fetch("url")
  .then(function(response) {
    //se ejecuta cuando todo sale bien
    //response: lo que devuelve el api
  })
  .catch(function(response) {
    //se ejecuta cuando hay un error
    //response: mensaje de error del api
  })