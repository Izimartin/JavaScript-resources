myFunction() {
    myArray = [];

    console.log(myArray ? .lenght ? true : false);


    myArray = [{ "id": 1 }];

    console.log(myArray ? .[0] ? .id ? true : false);
    console.log(myArray ? .[0] ? .name ? true : false);

    console.log(myArray ? .[0] ? .name ? ? "No id property");

}