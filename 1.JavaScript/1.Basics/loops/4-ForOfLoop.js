// The for..of creates loop over the values of an iterable object including built-in Map, Array, Set, String, and TypedArray.

const array = ['apple', 'banana', 'orange'];

for (const value of array) {
    console.log(value);
}